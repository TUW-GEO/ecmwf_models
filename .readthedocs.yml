version: 2

sphinx:
  configuration: docs/conf.py

formats:
  - pdf

submodules:
  include: all

conda:
  environment: environment/latest.yml

python:
  version: 3.7
  install:
    - method: pip
      path: .
